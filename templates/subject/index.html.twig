{% extends 'base.html.twig' %}

{% block title %}Subject index{% endblock %}

{% block body %}
    <h1>Subject index</h1>
<a href="{{ path('admin_dashboard') }}"
   class="inline-flex items-center px-3 py-1.5 rounded border border-slate-200 bg-white text-slate-800 hover:bg-slate-50 text-sm">
  ← Back to Admin
</a>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Code</th>
                <th>Is_active</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for subject in subjects %}
            <tr>
                <td>{{ subject.id }}</td>
                <td>{{ subject.name }}</td>
                <td>{{ subject.code }}</td>
                <td>{{ subject.isActive ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_subject_show', {'id': subject.id}) }}">show</a>
                    <a href="{{ path('app_subject_edit', {'id': subject.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_subject_new') }}">Create new</a>
{% endblock %}
