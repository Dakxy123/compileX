{% extends 'base.html.twig' %}

{% block title %}Edit Enrollment — Admin{% endblock %}

{% block body %}
<script src="https://cdn.tailwindcss.com"></script>

<div class="p-8 max-w-4xl mx-auto">
  <h1 class="text-3xl font-semibold mb-4">Edit Enrollment</h1>

  <div class="flex justify-between mb-4">
    <a href="{{ path('app_enrollment_index') }}"
       class="text-blue-600 hover:underline">&larr; Back to list</a>

    <div>
      {% include 'enrollment/_delete_form.html.twig' %}
    </div>
  </div>

  <div class="bg-white dark:bg-gray-800 p-6 rounded shadow border border-gray-200 dark:border-gray-700">
    {% include 'enrollment/_form.html.twig' with { 'button_label': 'Update' } %}
  </div>
</div>
{% endblock %}
